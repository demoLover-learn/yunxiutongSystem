<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.mapper.ServiceCategoryMapper">
    <insert id="insert" parameterType="org.example.vo.ServiceCategoryVO">
        insert into service_category(name, sort, status, create_time, update_time) VALUES
            (#{name},#{sort},#{status},#{createTime},#{updateTime})
    </insert>

    <update id="update" parameterType="org.example.entity.ServiceCategory">
        update service_category
        <set>
            <if test="name!=null">
                name=#{name},
            </if>
            <if test="sort!=null">
                sort=#{sort},
            </if>
        <if test="status!=null">
            status=#{status}
        </if>
        </set>
        where id =#{id}
    </update>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.mapper.ServiceOrderLogMapper">

    <insert id="insert" parameterType="org.example.entity.ServiceOrderLog">
        insert into service_order_log(order_id, operator_type, operator_id, from_status,
                                      to_status, content, create_time) VALUES
                (#{orderId},#{operatorType},#{operatorId},#{fromStatus},#{toStatus},#{content},#{createTime})
    </insert>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.example.mapper.UserOrderCommentMapper">
    <insert id="insert" parameterType="org.example.entity.ServiceOrderComment">
        insert into service_order_comment
        (order_id, user_id, worker_id, score, content, create_time) VALUES
         (#{orderId},#{userId},#{workerId},#{score},#{content},#{createTime})
    </insert>
    <update id="update" parameterType="org.example.entity.ServiceOrderComment">
        update service_order_comment
        <set>
            <if test="orderId!=null">order_id=#{orderId},</if>
            <if test="userId!=null">user_id=#{userId},</if>
            <if test="workerId!=null">worker_id=#{workerId},</if>
            <if test="score!=null">score=#{score},</if>
            <if test="content!=null">content=#{content},</if>
            <if test="createTime!=null">create_time=#{createTime}</if>
        </set>
        where
            id=#{id}
    </update>
</mapper>